const unauthenticatedRoutes = ["/", "/team", "/workshop", "/login", "/logout/success"]


// In the case of URLS that have slugs and those slugs must also be unauthenticated
export const prefixes = ["/workshop"];

export default unauthenticatedRoutes