import { StatCards } from "./StatCards";
import CreateQuestion from "./CreateQuestion";

export const Grid = () => {
  return (
    <div className="px-4">
      <StatCards />
      <CreateQuestion />
    </div>

  );
};
