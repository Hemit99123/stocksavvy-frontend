export interface TestimonialCardProps {
    color: "green" | "cyan" | "yellow",
    text: string;
    name: string;
    date: string;
    rotate: string; // Rotation value, e.g., 'rotate-2', 'rotate-6'
}